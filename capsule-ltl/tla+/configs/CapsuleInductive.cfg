\* Config for Apalache inductive-invariant checking of CapsuleInductive.tla.
\* Apalache does NOT use INVARIANT/PROPERTY the way TLC does; the checks are
\* driven from the command line (see CapsuleInductive.sh). This .cfg only
\* fixes the constant so a plain `tlc` smoke-test can also parse the module.
CONSTANTS
    CRASH = FALSE
